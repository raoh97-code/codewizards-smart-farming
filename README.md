Steps to run the project :-
    1. check
       python --version
    2. if python exists, run
        cd backend
        pip install --upgrade pip
        pip install -r requirements.txt
        python app.py
    3. goto "https://127.0.0.1:5000"